# Frontend (Vite + React)

Minimal React UI to display Categories and Products from the Django API.

## Configure

Create a `.env.local` if needed to point to your backend:

```
VITE_API_BASE=http://localhost:8000
```

## Run

```powershell
npm install
npm run dev
```

Open http://localhost:5173
